trigger:
  branches:
    include:
      - main

variables:
  - name: branch
    value: 'main'
  - name: pantheon-branch
    value: 'main'
  - name: vmImageName
    value: 'ubuntu-latest'

pool:
  name: 'azureagent'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: BuildAndDeploy
    displayName: 'Build and Deploy'
    steps:
    - checkout: self

    - script: |
        echo "Running build steps..."
      displayName: 'Build Step'

- stage: DeployToProduction
  displayName: 'Deploy to Production'
  dependsOn: Build
  condition: or(and(succeeded('manual_approval'), or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master'))), and(succeeded('auto_deploy'), not(or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master')))))

  jobs:
  - job: manual_approval
    displayName: "Manual Approval"
    pool:
      name: 'server'
    condition: or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master'))
    steps:
    - task: ManualValidation@0
      inputs:
        instructions: 'Deployment requires manual approval. Please review and approve to continue.'
        onTimeout: 'resume'
        notifyUsers: "sa"
      displayName: 'Manual Intervention for Master Branch Deployment'

  - job: auto_deploy
    displayName: 'Automatic Deployment'
    pool:
      vmImage: $(vmImageName)
    condition: and(succeededOrFailed('BuildAndDeploy'), not(or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master'))))
    steps:
    - script: |
        echo "Running automatic deployment steps for non-master branch..."
      displayName: 'Automatic Deployment Step'

  - job: deploy_to_production
    displayName: 'Deploy to Production'
    dependsOn: [manual_approval, auto_deploy]
    condition: or(and(succeeded('manual_approval'), or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master'))), and(succeeded('auto_deploy'), not(or(eq(variables['branch'], 'master'), eq(variables['pantheon-branch'], 'master')))))
    pool:
      vmImage: $(vmImageName)
    steps:
    - script: |
        echo "Deployment to production completed."
      displayName: 'Deployment to Production Step'
